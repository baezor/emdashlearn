---
interface Props {
	size?: "sm" | "md" | "lg" | "hero";
}
const { size = "md" } = Astro.props;
---

<span class={`wordmark wordmark--${size}`} aria-label="EmDash Learn">
	<span class="wordmark__prefix">EmDash</span><span class="wordmark__dash">—</span><span
		class="wordmark__suffix">Learn</span
	>
</span>

<style>
	.wordmark {
		display: inline-flex;
		align-items: baseline;
		font-family: var(--font-display);
		font-weight: 800;
		letter-spacing: -0.03em;
		line-height: 1;
		user-select: none;
	}

	.wordmark__prefix,
	.wordmark__suffix {
		color: var(--color-text);
	}

	.wordmark__dash {
		font-family: var(--font-mono);
		font-weight: 700;
		color: var(--color-accent);
		animation: cursor-blink 1.2s step-end infinite;
		/* Prevent layout shift from animation */
		display: inline-block;
	}

	/* Sizes */
	.wordmark--sm {
		font-size: 1rem;
	}

	.wordmark--md {
		font-size: 1.5rem;
	}

	.wordmark--lg {
		font-size: 2rem;
	}

	.wordmark--hero {
		font-size: clamp(1rem, 6.4vw, 6.5rem);
		max-width: 100%;
		overflow: hidden;
	}
</style>
